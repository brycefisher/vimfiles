snippet deb Creates a debug block
	//DEBUG
	var_dump(${1:$var});
	exit(${2:'test'});
	//DEBUG

snippet debif Creates a conditional debug block
	//DEBUG
	if (${1:$var} == ${2:$val}) {
		var_dump($1);
		exit($2);
	}
	//DEBUG

snippet funct Creates a Drupally function
	/**
	 * Helper function that ${1:...}
	 *
	 * @param ${2:obj} ${3:$node} - ${4:A node object}
	 * @return ${5:str}
	 */
	function ${6:myfunction}($3) {
		${7://Write implementation}
	}
